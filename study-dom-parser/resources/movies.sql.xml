
      create table movies (
           movieId int not null auto_increment,
           imdb char(10) not null unique,
           title char(255) not null,
           year int(4) not null,
           primary key(movieId) 
      ) engine=InnoDB;
      create table genres (       
           genreId int not null auto_increment,    
           name char(16) not null unique,
           primary key(genreId) 
      ) engine=InnoDB;
      create table directors (
           directorId int not null auto_increment,
           imdb char(10) not null unique,
           name char(255) not null,
           primary key(directorId) 
      ) engine=InnoDB;
      create table moviegenres (       
           id int not null auto_increment,    
           movieId int not null,
           genreId int not null,
           primary key(id) 
      ) engine=InnoDB;
      create table moviedirectors (
           id int not null auto_increment,
           movieId int not null,
           directorId int not null,
           primary key(id) 
      ) engine=InnoDB;
      insert into genres(name) values ('Action'),('Adventure'),('Animation'),('Anime'),('Biography'),('Cartoons'),
                                      ('Comedy'),('Crime'),('Disaster'),('Documentary'),('Drama'),('Family'),('Fantasy'),
                                      ('Film-noir'),('Horror'),('Musical'),('Mystery'),('Romance'),('Sci-fi'),('Thriller'),
                                      ('War'),('Western');
       
          insert into movies(imdb,title,`year`) values ('tt1022603','500 Days Of Summer',2009);
          select movieId into @mid from movies where imdb = 'tt1022603';
           
             insert into directors values (NULL,'nm1989536','Marc Webb');
             select directorId into @did from directors where imdb='nm1989536';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1183251','Beyond a Reasonable Doubt',2009);
          select movieId into @mid from movies where imdb = 'tt1183251';
           
             insert into directors values (NULL,'nm0001382','Peter Hyams');
             select directorId into @did from directors where imdb='nm0001382';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1034032','Gamer',2009);
          select movieId into @mid from movies where imdb = 'tt1034032';
           
             insert into directors values (NULL,'nm0004410','Mark Neveldine');
             select directorId into @did from directors where imdb='nm0004410';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
           
             insert into directors values (NULL,'nm0962729','Brian Taylor');
             select directorId into @did from directors where imdb='nm0962729';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1179258','Cheri',2009);
          select movieId into @mid from movies where imdb = 'tt1179258';
           
             insert into directors values (NULL,'nm0001241','Stephen Frears');
             select directorId into @did from directors where imdb='nm0001241';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1235124','Dorian Gray',2009);
          select movieId into @mid from movies where imdb = 'tt1235124';
           
             insert into directors values (NULL,'nm0662529','Oliver Parker');
             select directorId into @did from directors where imdb='nm0662529';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0361748','Inglourious Basterds',2009);
          select movieId into @mid from movies where imdb = 'tt0361748';
           
             insert into directors values (NULL,'nm0000233','Quentin Tarantino');
             select directorId into @did from directors where imdb='nm0000233';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'War';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1057500','Invictus',2009);
          select movieId into @mid from movies where imdb = 'tt1057500';
           
             insert into directors values (NULL,'nm0000142','Clint Eastwood');
             select directorId into @did from directors where imdb='nm0000142';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'History';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1135503','Julie & Julia',2009);
          select movieId into @mid from movies where imdb = 'tt1135503';
           
             insert into directors values (NULL,'nm0001188','Nora Ephron');
             select directorId into @did from directors where imdb='nm0001188';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0913425','Los abrazos rotos',2009);
          select movieId into @mid from movies where imdb = 'tt0913425';
           
             insert into directors values (NULL,'nm0000264','Pedro Almodovar');
             select directorId into @did from directors where imdb='nm0000264';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1230414','It is Complicated',2009);
          select movieId into @mid from movies where imdb = 'tt1230414';
           
             insert into directors values (NULL,'nm0583600','Nancy Meyers');
             select directorId into @did from directors where imdb='nm0583600';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0105046','Of Mice and Men',1992);
          select movieId into @mid from movies where imdb = 'tt0105046';
           
             insert into directors values (NULL,'nm0000641','Gary Sinise');
             select directorId into @did from directors where imdb='nm0000641';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0913354','Armored',2009);
          select movieId into @mid from movies where imdb = 'tt0913354';
           
             insert into directors values (NULL,'nm0030735','Nimród Antal');
             select directorId into @did from directors where imdb='nm0030735';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1548542','Bornova Bornova',2009);
          select movieId into @mid from movies where imdb = 'tt1548542';
           
             insert into directors values (NULL,'nm3692009','Inan Temelkuran');
             select directorId into @did from directors where imdb='nm3692009';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1035736','Coco avant Chanel',2009);
          select movieId into @mid from movies where imdb = 'tt1035736';
           
             insert into directors values (NULL,'nm0284774','Anne Fontaine');
             select directorId into @did from directors where imdb='nm0284774';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1171701','Nefes: Vatan sagolsun',2009);
          select movieId into @mid from movies where imdb = 'tt1171701';
           
             insert into directors values (NULL,'nm2896395','Levent Semerci');
             select directorId into @did from directors where imdb='nm2896395';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'War';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1049413','Up',2009);
          select movieId into @mid from movies where imdb = 'tt1049413';
           
             insert into directors values (NULL,'nm0230032','Pete Docter');
             select directorId into @did from directors where imdb='nm0230032';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Animation';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Family';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0365929','Whiteout',2009);
          select movieId into @mid from movies where imdb = 'tt0365929';
           
             insert into directors values (NULL,'nm0784061','Dominic Sena');
             select directorId into @did from directors where imdb='nm0784061';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0452694','The Time Travelers Wife',2009);
          select movieId into @mid from movies where imdb = 'tt0452694';
           
             insert into directors values (NULL,'nm0777881','Robert Schwentke');
             select directorId into @did from directors where imdb='nm0777881';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1178663','Whatever Works',2009);
          select movieId into @mid from movies where imdb = 'tt1178663';
           
             insert into directors values (NULL,'nm0000095','Woody Allen');
             select directorId into @did from directors where imdb='nm0000095';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1035730','Anonyma - Eine Frau in Berlin ',2009);
          select movieId into @mid from movies where imdb = 'tt1035730';
           
             insert into directors values (NULL,'nm0299621','Max Färberböck');
             select directorId into @did from directors where imdb='nm0299621';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'War';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1156398','Zombieland',2009);
          select movieId into @mid from movies where imdb = 'tt1156398';
           
             insert into directors values (NULL,'nm0281508','Ruben Fleischer');
             select directorId into @did from directors where imdb='nm0281508';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1085515','Weather Girl',2009);
          select movieId into @mid from movies where imdb = 'tt1085515';
           
             insert into directors values (NULL,'nm0915814','Blayne Weaver');
             select directorId into @did from directors where imdb='nm0915814';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0409459','Watchmen',2009);
          select movieId into @mid from movies where imdb = 'tt0409459';
           
             insert into directors values (NULL,'nm0811583','Zack Snyder');
             select directorId into @did from directors where imdb='nm0811583';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0479341','Adam Resurrected',2008);
          select movieId into @mid from movies where imdb = 'tt0479341';
           
             insert into directors values (NULL,'nm0001707','Paul Schrader');
             select directorId into @did from directors where imdb='nm0001707';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'War';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0808151','Angels & Deamons',2009);
          select movieId into @mid from movies where imdb = 'tt0808151';
           
             insert into directors values (NULL,'nm0000165','Ron Howard');
             select directorId into @did from directors where imdb='nm0000165';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1176740','Away We Go',2009);
          select movieId into @mid from movies where imdb = 'tt1176740';
           
             insert into directors values (NULL,'nm0005222','Sam Mendes');
             select directorId into @did from directors where imdb='nm0005222';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1080016','Ice Age: Dawn of the Dinosaurs',2009);
          select movieId into @mid from movies where imdb = 'tt1080016';
           
             insert into directors values (NULL,'nm0757858','Carlos Saldanha');
             select directorId into @did from directors where imdb='nm0757858';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Animation';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Family';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1235142','Last Ride',2009);
          select movieId into @mid from movies where imdb = 'tt1235142';
           
             insert into directors values (NULL,'nm1380395','Glendyn Ivin');
             select directorId into @did from directors where imdb='nm1380395';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0926380','The Boys Are Back',2009);
          select movieId into @mid from movies where imdb = 'tt0926380';
           
             insert into directors values (NULL,'nm0382956','Scott Hicks');
             select directorId into @did from directors where imdb='nm0382956';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1073241','Nothing But the Truth',2008);
          select movieId into @mid from movies where imdb = 'tt1073241';
           
             insert into directors values (NULL,'nm0527109','Rod Lurie');
             select directorId into @did from directors where imdb='nm0527109';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0899128','100 Feet',2008);
          select movieId into @mid from movies where imdb = 'tt0899128';
           
             insert into directors values (NULL,'nm0714599','Eric Red');
             select directorId into @did from directors where imdb='nm0714599';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0471041','The Tournament',2009);
          select movieId into @mid from movies where imdb = 'tt0471041';
           
             insert into directors values (NULL,'nm1470993','Scott Mann');
             select directorId into @did from directors where imdb='nm1470993';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0844457','Nordwand, aka North Face (International: English
			title)',2008);
          select movieId into @mid from movies where imdb = 'tt0844457';
           
             insert into directors values (NULL,'nm0836715','Philipp Stolzl');
             select directorId into @did from directors where imdb='nm0836715';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'History';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sport';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1019452','A Serious Man',2009);
          select movieId into @mid from movies where imdb = 'tt1019452';
           
             insert into directors values (NULL,'nm0001053','Ethan Coen');
             select directorId into @did from directors where imdb='nm0001053';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
           
             insert into directors values (NULL,'nm0001054','Joel Coen');
             select directorId into @did from directors where imdb='nm0001054';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1233227','Saw VI',2009);
          select movieId into @mid from movies where imdb = 'tt1233227';
           
             insert into directors values (NULL,'nm0340436','Kevin Greutert');
             select directorId into @did from directors where imdb='nm0340436';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1075113','Ne te retourne pas',2009);
          select movieId into @mid from movies where imdb = 'tt1075113';
           
             insert into directors values (NULL,'nm0888418','Marina de Van');
             select directorId into @did from directors where imdb='nm0888418';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = '';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1136608','District 9',2009);
          select movieId into @mid from movies where imdb = 'tt1136608';
           
             insert into directors values (NULL,'nm0088955','Neill Blomkamp');
             select directorId into @did from directors where imdb='nm0088955';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1225822','Extract',2009);
          select movieId into @mid from movies where imdb = 'tt1225822';
           
             insert into directors values (NULL,'nm0431918','Mike Judge');
             select directorId into @did from directors where imdb='nm0431918';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1238291','Five Minutes of Haven',2009);
          select movieId into @mid from movies where imdb = 'tt1238291';
           
             insert into directors values (NULL,'nm0386570','Oliver Hirschbiegel');
             select directorId into @did from directors where imdb='nm0386570';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1143110','High Life',2009);
          select movieId into @mid from movies where imdb = 'tt1143110';
           
             insert into directors values (NULL,'nm0946750','Gary Yates');
             select directorId into @did from directors where imdb='nm0946750';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1041829','The Proposal',2009);
          select movieId into @mid from movies where imdb = 'tt1041829';
           
             insert into directors values (NULL,'nm0281945','Anne Fletcher');
             select directorId into @did from directors where imdb='nm0281945';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1068678','Veronika Decides to Die',2009);
          select movieId into @mid from movies where imdb = 'tt1068678';
           
             insert into directors values (NULL,'nm1369721','Emily Young');
             select directorId into @did from directors where imdb='nm1369721';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0974613','The Other Man',2008);
          select movieId into @mid from movies where imdb = 'tt0974613';
           
             insert into directors values (NULL,'nm0264236','Richard Eyre');
             select directorId into @did from directors where imdb='nm0264236';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1092633','The Goods: Live Hard, Sell Hard',2009);
          select movieId into @mid from movies where imdb = 'tt1092633';
           
             insert into directors values (NULL,'nm0107366','Neal Brennan');
             select directorId into @did from directors where imdb='nm0107366';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1119646','The Hangover',2009);
          select movieId into @mid from movies where imdb = 'tt1119646';
           
             insert into directors values (NULL,'nm0680846','Todd Phillips');
             select directorId into @did from directors where imdb='nm0680846';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1133993','Serious Moonlight',2009);
          select movieId into @mid from movies where imdb = 'tt1133993';
           
             insert into directors values (NULL,'nm0385644','Cheryl Hines');
             select directorId into @did from directors where imdb='nm0385644';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1152836','Public Enemies',2009);
          select movieId into @mid from movies where imdb = 'tt1152836';
           
             insert into directors values (NULL,'nm0000520','Micheal Mann');
             select directorId into @did from directors where imdb='nm0000520';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'History';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0974014','Creation',2009);
          select movieId into @mid from movies where imdb = 'tt0974014';
           
             insert into directors values (NULL,'nm0000750','Jon Amiel');
             select directorId into @did from directors where imdb='nm0000750';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1129445','Amelia',2009);
          select movieId into @mid from movies where imdb = 'tt1129445';
           
             insert into directors values (NULL,'nm0619762','Mira Nair');
             select directorId into @did from directors where imdb='nm0619762';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1205535','The Rebound',2009);
          select movieId into @mid from movies where imdb = 'tt1205535';
           
             insert into directors values (NULL,'nm0294505','Bart Freundlich');
             select directorId into @did from directors where imdb='nm0294505';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1032819','Powder Blue',2009);
          select movieId into @mid from movies where imdb = 'tt1032819';
           
             insert into directors values (NULL,'nm0119613','Timothy Linh Bui');
             select directorId into @did from directors where imdb='nm0119613';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1234548','The Men Who Stare at Goats',2009);
          select movieId into @mid from movies where imdb = 'tt1234548';
           
             insert into directors values (NULL,'nm0381416','Grant Heslov');
             select directorId into @did from directors where imdb='nm0381416';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'War';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0810784','Bright Star',2009);
          select movieId into @mid from movies where imdb = 'tt0810784';
           
             insert into directors values (NULL,'nm0001005','Jane Campion');
             select directorId into @did from directors where imdb='nm0001005';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0795351','Case 39',2009);
          select movieId into @mid from movies where imdb = 'tt0795351';
           
             insert into directors values (NULL,'nm0023355','Christian Alvert');
             select directorId into @did from directors where imdb='nm0023355';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1127877','Cold Souls',2009);
          select movieId into @mid from movies where imdb = 'tt1127877';
           
             insert into directors values (NULL,'nm1754436','Sophie Barthes');
             select directorId into @did from directors where imdb='nm1754436';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1182345','Moon',2009);
          select movieId into @mid from movies where imdb = 'tt1182345';
           
             insert into directors values (NULL,'nm1512910','Duncan Jones');
             select directorId into @did from directors where imdb='nm1512910';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1262981','Worlds Greatest Dad',2009);
          select movieId into @mid from movies where imdb = 'tt1262981';
           
             insert into directors values (NULL,'nm0001281','Bobcat Goldthwait');
             select directorId into @did from directors where imdb='nm0001281';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1179904','Paranormal Activity',2007);
          select movieId into @mid from movies where imdb = 'tt1179904';
           
             insert into directors values (NULL,'nm2305431','Oren Peli');
             select directorId into @did from directors where imdb='nm2305431';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0473705','State of Play',2009);
          select movieId into @mid from movies where imdb = 'tt0473705';
           
             insert into directors values (NULL,'nm0531817','Kevin Macdonald');
             select directorId into @did from directors where imdb='nm0531817';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1173494','God on Trial',2008);
          select movieId into @mid from movies where imdb = 'tt1173494';
           
             insert into directors values (NULL,'nm0214320','Andy DeEmmony');
             select directorId into @did from directors where imdb='nm0214320';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0844286','The Brothers Bloom',2009);
          select movieId into @mid from movies where imdb = 'tt0844286';
           
             insert into directors values (NULL,'nm0426059','Rian Johnson');
             select directorId into @did from directors where imdb='nm0426059';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1185431','My One and Only',2009);
          select movieId into @mid from movies where imdb = 'tt1185431';
           
             insert into directors values (NULL,'nm0518644','Richard Loncraine');
             select directorId into @did from directors where imdb='nm0518644';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1132620','Män Som Hatar Kvinnor',2009);
          select movieId into @mid from movies where imdb = 'tt1132620';
           
             insert into directors values (NULL,'nm0649117','Niels Arden Oplev');
             select directorId into @did from directors where imdb='nm0649117';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0978762','Mary and Max',2009);
          select movieId into @mid from movies where imdb = 'tt0978762';
           
             insert into directors values (NULL,'nm0254178','Adam Elliot');
             select directorId into @did from directors where imdb='nm0254178';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Animation';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0978762','The Limits of Control',2009);
          select movieId into @mid from movies where imdb = 'tt0978762';
           
             insert into directors values (NULL,'nm0000464','Jim Jarmusch');
             select directorId into @did from directors where imdb='nm0000464';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0862846','Sunshine Cleaning',2008);
          select movieId into @mid from movies where imdb = 'tt0862846';
           
             insert into directors values (NULL,'nm0420422','Christine Jeffs');
             select directorId into @did from directors where imdb='nm0420422';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0971209','A Perfect Getaway',2009);
          select movieId into @mid from movies where imdb = 'tt0971209';
           
             insert into directors values (NULL,'nm0878638','David Twohy');
             select directorId into @did from directors where imdb='nm0878638';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1078588','My Sisters Keeper',2009);
          select movieId into @mid from movies where imdb = 'tt1078588';
           
             insert into directors values (NULL,'nm0001024','Nick Cassavetes');
             select directorId into @did from directors where imdb='nm0001024';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0795441','Oorlogswinter',2008);
          select movieId into @mid from movies where imdb = 'tt0795441';
           
             insert into directors values (NULL,'nm0465551','Martin Koolhoven');
             select directorId into @did from directors where imdb='nm0465551';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'History';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'War';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0762125','Planet 51',2009);
          select movieId into @mid from movies where imdb = 'tt0762125';
           
             insert into directors values (NULL,'nm3360961','Jorge Blanco');
             select directorId into @did from directors where imdb='nm3360961';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Animation';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Family';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Fantasy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0110882','Before the Rain',1994);
          select movieId into @mid from movies where imdb = 'tt0110882';
           
             insert into directors values (NULL,'nm0541391','Milcho Manchevski');
             select directorId into @did from directors where imdb='nm0541391';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'War';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1155056','I Love You, Man',2009);
          select movieId into @mid from movies where imdb = 'tt1155056';
           
             insert into directors values (NULL,'nm0357453','John Hamburg');
             select directorId into @did from directors where imdb='nm0357453';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0086182','Rembetiko',1983);
          select movieId into @mid from movies where imdb = 'tt0086182';
           
             insert into directors values (NULL,'nm0002948','Costas Ferris');
             select directorId into @did from directors where imdb='nm0002948';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'History';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Music';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'War';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1129445','Amelia',2009);
          select movieId into @mid from movies where imdb = 'tt1129445';
           
             insert into directors values (NULL,'nm0619762','Mira Nair');
             select directorId into @did from directors where imdb='nm0619762';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1226271','The Damned United',2009);
          select movieId into @mid from movies where imdb = 'tt1226271';
           
             insert into directors values (NULL,'nm0393799','Tom Hooper');
             select directorId into @did from directors where imdb='nm0393799';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sport';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0475296','Hiroshima',2005);
          select movieId into @mid from movies where imdb = 'tt0475296';
           
             insert into directors values (NULL,'nm0932882','Paul Wilmshurst');
             select directorId into @did from directors where imdb='nm0932882';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'History';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0808399','New York, I Love You',2009);
          select movieId into @mid from movies where imdb = 'tt0808399';
           
             insert into directors values (NULL,'nm0015359','Fatih Akin');
             select directorId into @did from directors where imdb='nm0015359';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
           
             insert into directors values (NULL,'nm0040939','Yvan Attal');
             select directorId into @did from directors where imdb='nm0040939';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1232776','Fish Tank',2009);
          select movieId into @mid from movies where imdb = 'tt1232776';
           
             insert into directors values (NULL,'nm0036349','Andrea Arnold');
             select directorId into @did from directors where imdb='nm0036349';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1130080','The Informant!',2009);
          select movieId into @mid from movies where imdb = 'tt1130080';
           
             insert into directors values (NULL,'nm0001752','Steven Soderbergh');
             select directorId into @did from directors where imdb='nm0001752';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1010278','The Courageous Heart of Irena Sendler',2009);
          select movieId into @mid from movies where imdb = 'tt1010278';
           
             insert into directors values (NULL,'nm0006867','John Kent Harrison');
             select directorId into @did from directors where imdb='nm0006867';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0768239','Storm',2009);
          select movieId into @mid from movies where imdb = 'tt0768239';
           
             insert into directors values (NULL,'nm0772691','Hans-Christian Schmid');
             select directorId into @did from directors where imdb='nm0772691';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0292963','Before the Devil Knows You are Dead',2007);
          select movieId into @mid from movies where imdb = 'tt0292963';
           
             insert into directors values (NULL,'nm0001486','Sidney Lumet');
             select directorId into @did from directors where imdb='nm0001486';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1187064','Triangle',2009);
          select movieId into @mid from movies where imdb = 'tt1187064';
           
             insert into directors values (NULL,'nm1247462','Christopher Smith');
             select directorId into @did from directors where imdb='nm1247462';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0057115','The Great Escape',1963);
          select movieId into @mid from movies where imdb = 'tt0057115';
           
             insert into directors values (NULL,'nm0836328','John Sturges');
             select directorId into @did from directors where imdb='nm0836328';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'War';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1190080','2012',2009);
          select movieId into @mid from movies where imdb = 'tt1190080';
           
             insert into directors values (NULL,'nm0000386','Roland Emmerich');
             select directorId into @did from directors where imdb='nm0000386';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1034302','The Cry of the Owl',2009);
          select movieId into @mid from movies where imdb = 'tt1034302';
           
             insert into directors values (NULL,'nm0861899','Jamie Thraves');
             select directorId into @did from directors where imdb='nm0861899';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1385824','13B',2009);
          select movieId into @mid from movies where imdb = 'tt1385824';
           
             insert into directors values (NULL,'nm3347823','Vikram K. Kumar');
             select directorId into @did from directors where imdb='nm3347823';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0036775','Double Indemnity',1944);
          select movieId into @mid from movies where imdb = 'tt0036775';
           
             insert into directors values (NULL,'nm0000697','Billy Wilder');
             select directorId into @did from directors where imdb='nm0000697';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Film-Noir';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0064115','Butch Cassidy and the Sundance Kid',1969);
          select movieId into @mid from movies where imdb = 'tt0064115';
           
             insert into directors values (NULL,'nm0001351','George Roy Hill');
             select directorId into @did from directors where imdb='nm0001351';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Western';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1305806','El secreto de sus ojos (a.k.a. The Secret in Their Eyes, International English title)',2009);
          select movieId into @mid from movies where imdb = 'tt1305806';
           
             insert into directors values (NULL,'nm0002728','Juan Jose Campanella');
             select directorId into @did from directors where imdb='nm0002728';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0083789','Danton',1983);
          select movieId into @mid from movies where imdb = 'tt0083789';
           
             insert into directors values (NULL,'nm0906667','Andrzej Wajda');
             select directorId into @did from directors where imdb='nm0906667';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'History';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0064276','Easy Rider',1969);
          select movieId into @mid from movies where imdb = 'tt0064276';
           
             insert into directors values (NULL,'nm0000454','Dennis Hopper');
             select directorId into @did from directors where imdb='nm0000454';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0986263','Surrogates',2009);
          select movieId into @mid from movies where imdb = 'tt0986263';
           
             insert into directors values (NULL,'nm0609236','Jonathan Mostow');
             select directorId into @did from directors where imdb='nm0609236';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0072890','Dog Day Afternoon',1975);
          select movieId into @mid from movies where imdb = 'tt0072890';
           
             insert into directors values (NULL,'nm0001486','Sidney Lumet');
             select directorId into @did from directors where imdb='nm0001486';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0044081','A Streetcar Named Desire',1951);
          select movieId into @mid from movies where imdb = 'tt0044081';
           
             insert into directors values (NULL,'nm0001415','Elia Kazan');
             select directorId into @did from directors where imdb='nm0001415';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0043014','Sunset Blvd.',1950);
          select movieId into @mid from movies where imdb = 'tt0043014';
           
             insert into directors values (NULL,'nm0000697','Billy Wilder');
             select directorId into @did from directors where imdb='nm0000697';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Film-Noir';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0074958','Network',1976);
          select movieId into @mid from movies where imdb = 'tt0074958';
           
             insert into directors values (NULL,'nm0001486','Sidney Lumet');
             select directorId into @did from directors where imdb='nm0001486';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0052357','Vertigo',1958);
          select movieId into @mid from movies where imdb = 'tt0052357';
           
             insert into directors values (NULL,'nm0000033','Alfred Hitchcock');
             select directorId into @did from directors where imdb='nm0000033';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0374546','Bom yeoreum gaeul gyeoul geurigo bom',2003);
          select movieId into @mid from movies where imdb = 'tt0374546';
           
             insert into directors values (NULL,'nm1104118','Ki-duk Kim');
             select directorId into @did from directors where imdb='nm1104118';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0047573','Them!',1954);
          select movieId into @mid from movies where imdb = 'tt0047573';
           
             insert into directors values (NULL,'nm0235066','Gordon Douglas');
             select directorId into @did from directors where imdb='nm0235066';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0423866','Bin-jip',2004);
          select movieId into @mid from movies where imdb = 'tt0423866';
           
             insert into directors values (NULL,'nm1104118','Ki-duk Kim');
             select directorId into @did from directors where imdb='nm1104118';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0428870','Nae meorisokui jiwoogae',2004);
          select movieId into @mid from movies where imdb = 'tt0428870';
           
             insert into directors values (NULL,'nm0497565','John H. Lee');
             select directorId into @did from directors where imdb='nm0497565';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0456470','Hwal',2005);
          select movieId into @mid from movies where imdb = 'tt0456470';
           
             insert into directors values (NULL,'nm1104118','Ki-duk Kim');
             select directorId into @did from directors where imdb='nm1104118';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1499666','Kimssi pyoryugi',2009);
          select movieId into @mid from movies where imdb = 'tt1499666';
           
             insert into directors values (NULL,'nm1536497','Hae-jun Lee');
             select directorId into @did from directors where imdb='nm1536497';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1366981','Uzak ihtimal',2009);
          select movieId into @mid from movies where imdb = 'tt1366981';
           
             insert into directors values (NULL,'nm3312722','Mahmut Fazil Coskun');
             select directorId into @did from directors where imdb='nm3312722';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0433362','Daybreakers',2009);
          select movieId into @mid from movies where imdb = 'tt0433362';
           
             insert into directors values (NULL,'nm1294961','Michael Spierig');
             select directorId into @did from directors where imdb='nm1294961';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
           
             insert into directors values (NULL,'nm1294962','Peter Spierig');
             select directorId into @did from directors where imdb='nm1294962';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1165253','Bin-mong',2008);
          select movieId into @mid from movies where imdb = 'tt1165253';
           
             insert into directors values (NULL,'nm1104118','Ki-duk Kim');
             select directorId into @did from directors where imdb='nm1104118';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Fantasy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0364569','Oldboy',2003);
          select movieId into @mid from movies where imdb = 'tt0364569';
           
             insert into directors values (NULL,'nm0661791','Chan-wook Park');
             select directorId into @did from directors where imdb='nm0661791';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0896529','Cairo Time',2009);
          select movieId into @mid from movies where imdb = 'tt0896529';
           
             insert into directors values (NULL,'nm0618779','Ruba Nadda');
             select directorId into @did from directors where imdb='nm0618779';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1286165','Pazar - Bir Ticaret Masalı',2008);
          select movieId into @mid from movies where imdb = 'tt1286165';
           
             insert into directors values (NULL,'nm0394114','Ben Hopkins');
             select directorId into @did from directors where imdb='nm0394114';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1313104','The Cove',2009);
          select movieId into @mid from movies where imdb = 'tt1313104';
           
             insert into directors values (NULL,'nm3174775','Louie Psihoyos');
             select directorId into @did from directors where imdb='nm3174775';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Documentary';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0494864','Tenderness',2009);
          select movieId into @mid from movies where imdb = 'tt0494864';
           
             insert into directors values (NULL,'nm0689852','John Polson');
             select directorId into @did from directors where imdb='nm0689852';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1028532','Hachiko: A Dogs Story',2009);
          select movieId into @mid from movies where imdb = 'tt1028532';
           
             insert into directors values (NULL,'nm0002120','Lasse Hallström');
             select directorId into @did from directors where imdb='nm0002120';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Family';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0040897','The Treasure of the Sierra Madre',1948);
          select movieId into @mid from movies where imdb = 'tt0040897';
           
             insert into directors values (NULL,'nm0001379','John Huston');
             select directorId into @did from directors where imdb='nm0001379';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Western';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0070354','Le magnifique',1973);
          select movieId into @mid from movies where imdb = 'tt0070354';
           
             insert into directors values (NULL,'nm0003606','Philippe de Broca');
             select directorId into @did from directors where imdb='nm0003606';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Fantasy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0063415','The Party',1968);
          select movieId into @mid from movies where imdb = 'tt0063415';
           
             insert into directors values (NULL,'nm0001175','Blake Edwards');
             select directorId into @did from directors where imdb='nm0001175';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0362478','The Box',2009);
          select movieId into @mid from movies where imdb = 'tt0362478';
           
             insert into directors values (NULL,'nm0446819','Richard Kelly');
             select directorId into @did from directors where imdb='nm0446819';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0780511','Everybodys Fine',2009);
          select movieId into @mid from movies where imdb = 'tt0780511';
           
             insert into directors values (NULL,'nm0428600','Kirk Jones');
             select directorId into @did from directors where imdb='nm0428600';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1144551','Peter and Vandy',2009);
          select movieId into @mid from movies where imdb = 'tt1144551';
           
             insert into directors values (NULL,'nm2842549','Jay DiPietro');
             select directorId into @did from directors where imdb='nm2842549';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0054215','Psycho',1960);
          select movieId into @mid from movies where imdb = 'tt0054215';
           
             insert into directors values (NULL,'nm0000033','Alfred Hitchcock');
             select directorId into @did from directors where imdb='nm0000033';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1247704','Women in Trouble',2009);
          select movieId into @mid from movies where imdb = 'tt1247704';
           
             insert into directors values (NULL,'nm0349406','Sebastian Gutierrez');
             select directorId into @did from directors where imdb='nm0349406';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0047478','Shichinin no samurai',1954);
          select movieId into @mid from movies where imdb = 'tt0047478';
           
             insert into directors values (NULL,'nm0000041','Akira Kurosawa');
             select directorId into @did from directors where imdb='nm0000041';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0293715','Yeopgijeogin geunyeo',2001);
          select movieId into @mid from movies where imdb = 'tt0293715';
           
             insert into directors values (NULL,'nm1030706','Jae-young Kwak');
             select directorId into @did from directors where imdb='nm1030706';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0397619','Samaria',2004);
          select movieId into @mid from movies where imdb = 'tt0397619';
           
             insert into directors values (NULL,'nm1104118','Ki-duk Kim');
             select directorId into @did from directors where imdb='nm1104118';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0046912','Dial M for Murder',1954);
          select movieId into @mid from movies where imdb = 'tt0046912';
           
             insert into directors values (NULL,'nm0000033','Alfred Hitchcock');
             select directorId into @did from directors where imdb='nm0000033';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1235166','Un prophète',2009);
          select movieId into @mid from movies where imdb = 'tt1235166';
           
             insert into directors values (NULL,'nm0002191','Jacques Audiard');
             select directorId into @did from directors where imdb='nm0002191';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1183921','The Vicious Kind',2009);
          select movieId into @mid from movies where imdb = 'tt1183921';
           
             insert into directors values (NULL,'nm1767218','Lee Toland Krieger');
             select directorId into @did from directors where imdb='nm1767218';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0762073','Bakjwi',2009);
          select movieId into @mid from movies where imdb = 'tt0762073';
           
             insert into directors values (NULL,'nm0661791','Chan-wook Park');
             select directorId into @did from directors where imdb='nm0661791';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1193138','Up in the air',2009);
          select movieId into @mid from movies where imdb = 'tt1193138';
           
             insert into directors values (NULL,'nm0718646','Jason Reitman');
             select directorId into @did from directors where imdb='nm0718646';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0074806','The Little Girl Who Lives Down the Lane',1976);
          select movieId into @mid from movies where imdb = 'tt0074806';
           
             insert into directors values (NULL,'nm0004654','Nicolas Gessner');
             select directorId into @did from directors where imdb='nm0004654';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1197624','Law Abiding Citizen',2009);
          select movieId into @mid from movies where imdb = 'tt1197624';
           
             insert into directors values (NULL,'nm0336620','F. Gary Gray');
             select directorId into @did from directors where imdb='nm0336620';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0875034','Nine',2009);
          select movieId into @mid from movies where imdb = 'tt0875034';
           
             insert into directors values (NULL,'nm0551128','Rob Marshall');
             select directorId into @did from directors where imdb='nm0551128';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Musical';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('','The Penguins of Madagascar Operation: DVD Premier
		',2010);
          select movieId into @mid from movies where imdb = '';
          
             select genreId into @gid from genres where name = 'Animation';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Family';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0887912','The Hurt Locker',2008);
          select movieId into @mid from movies where imdb = 'tt0887912';
           
             insert into directors values (NULL,'nm0000941','Kathryn Bigelow');
             select directorId into @did from directors where imdb='nm0000941';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'War';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0821642','The Soloist',2009);
          select movieId into @mid from movies where imdb = 'tt0821642';
           
             insert into directors values (NULL,'m0942504','Joe Wright');
             select directorId into @did from directors where imdb='m0942504';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Music';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1186830','Agora',2009);
          select movieId into @mid from movies where imdb = 'tt1186830';
           
             insert into directors values (NULL,'nm0024622','Alejandro Amenábar');
             select directorId into @did from directors where imdb='nm0024622';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'History';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1220220','Motherhood',2009);
          select movieId into @mid from movies where imdb = 'tt1220220';
           
             insert into directors values (NULL,'nm0225869','Katherine Dieckmann');
             select directorId into @did from directors where imdb='nm0225869';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0065214','The Wild Brunch',1969);
          select movieId into @mid from movies where imdb = 'tt0065214';
           
             insert into directors values (NULL,'nm0001603','Sam Peckinpah');
             select directorId into @did from directors where imdb='nm0001603';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Western';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1523515','Neseli Hayat',2009);
          select movieId into @mid from movies where imdb = 'tt1523515';
           
             insert into directors values (NULL,'nm0258784','Yilmaz Erdogan');
             select directorId into @did from directors where imdb='nm0258784';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1226232','The Greatest',2009);
          select movieId into @mid from movies where imdb = 'tt1226232';
           
             insert into directors values (NULL,'nm0275277','Shana Feste');
             select directorId into @did from directors where imdb='nm0275277';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1370212','My Only Sunshine',2008);
          select movieId into @mid from movies where imdb = 'tt1370212';
           
             insert into directors values (NULL,'nm0258732','Reha Erdem');
             select directorId into @did from directors where imdb='nm0258732';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0094608','The Accused',1988);
          select movieId into @mid from movies where imdb = 'tt0094608';
           
             insert into directors values (NULL,'nm0438279','Jonathan Kaplan');
             select directorId into @did from directors where imdb='nm0438279';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0092965','Empire of the Sun',1987);
          select movieId into @mid from movies where imdb = 'tt0092965';
           
             insert into directors values (NULL,'nm0000229','Steven Spielberg');
             select directorId into @did from directors where imdb='nm0000229';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'War';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0086066','The Outsiders',1983);
          select movieId into @mid from movies where imdb = 'tt0086066';
           
             insert into directors values (NULL,'nm0000338','Francis Ford Coppola');
             select directorId into @did from directors where imdb='nm0000338';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1300851','The Boondock Saints II: All Saints Day',2009);
          select movieId into @mid from movies where imdb = 'tt1300851';
           
             insert into directors values (NULL,'nm0240627','Troy Duffy');
             select directorId into @did from directors where imdb='nm0240627';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1179034','From Paris with Love',2010);
          select movieId into @mid from movies where imdb = 'tt1179034';
           
             insert into directors values (NULL,'nm0603628','Pierre Morel');
             select directorId into @did from directors where imdb='nm0603628';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0110005','Heavenly Creatures',1994);
          select movieId into @mid from movies where imdb = 'tt0110005';
           
             insert into directors values (NULL,'nm0001392','Peter Jackson');
             select directorId into @did from directors where imdb='nm0001392';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1220719','Yip Man',2008);
          select movieId into @mid from movies where imdb = 'tt1220719';
           
             insert into directors values (NULL,'nm0948159','Wilson Yip');
             select directorId into @did from directors where imdb='nm0948159';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1134629','The Private Lives of Pippa Lee',2009);
          select movieId into @mid from movies where imdb = 'tt1134629';
           
             insert into directors values (NULL,'nm0589182','Rebecca Miller');
             select directorId into @did from directors where imdb='nm0589182';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1054606','The Imaginarium of Doctor Parnassus',2009);
          select movieId into @mid from movies where imdb = 'tt1054606';
           
             insert into directors values (NULL,'nm0000416','Terry Gilliam');
             select directorId into @did from directors where imdb='nm0000416';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Fantasy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1234548','The Men Who Stare at Goats',2009);
          select movieId into @mid from movies where imdb = 'tt1234548';
           
             insert into directors values (NULL,'nm0381416','Grant Heslov');
             select directorId into @did from directors where imdb='nm0381416';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'War';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0844471','Cloudy with a Chance of Meatballs',2009);
          select movieId into @mid from movies where imdb = 'tt0844471';
           
             insert into directors values (NULL,'nm0520488','Phil Lord');
             select directorId into @did from directors where imdb='nm0520488';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
           
             insert into directors values (NULL,'nm0588087','Chris Miller');
             select directorId into @did from directors where imdb='nm0588087';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Animation';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Family';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0780521','The Princess and the Frog',2009);
          select movieId into @mid from movies where imdb = 'tt0780521';
           
             insert into directors values (NULL,'nm0166256','Ron Clements');
             select directorId into @did from directors where imdb='nm0166256';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
           
             insert into directors values (NULL,'nm0615780','John Musker');
             select directorId into @did from directors where imdb='nm0615780';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Animation';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Family';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Fantasy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Musical';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1174732','An Education',2009);
          select movieId into @mid from movies where imdb = 'tt1174732';
           
             insert into directors values (NULL,'nm0771054','Lone Scherfig');
             select directorId into @did from directors where imdb='nm0771054';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0499549','Avatar',2009);
          select movieId into @mid from movies where imdb = 'tt0499549';
           
             insert into directors values (NULL,'nm0000116','James Cameron');
             select directorId into @did from directors where imdb='nm0000116';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0499549','Avatar 3D',2009);
          select movieId into @mid from movies where imdb = 'tt0499549';
           
             insert into directors values (NULL,'nm0000116','James Cameron');
             select directorId into @did from directors where imdb='nm0000116';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0929632','Precious: Based on the Novel Push by Sapphire
		',2009);
          select movieId into @mid from movies where imdb = 'tt0929632';
           
             insert into directors values (NULL,'nm0200005','Lee Daniels');
             select directorId into @did from directors where imdb='nm0200005';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0878804','The Blind Side',2009);
          select movieId into @mid from movies where imdb = 'tt0878804';
           
             insert into directors values (NULL,'nm0359387','John Lee Hancock');
             select directorId into @did from directors where imdb='nm0359387';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sport';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1259571','New Moon',2009);
          select movieId into @mid from movies where imdb = 'tt1259571';
           
             insert into directors values (NULL,'nm0919363','Chris Weitz');
             select directorId into @did from directors where imdb='nm0919363';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Fantasy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0432283','Fantastic Mr. Fox',2009);
          select movieId into @mid from movies where imdb = 'tt0432283';
           
             insert into directors values (NULL,'nm0027572','Wes Anderson');
             select directorId into @did from directors where imdb='nm0027572';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Animation';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Family';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0988045','Sherlock Holmes',2009);
          select movieId into @mid from movies where imdb = 'tt0988045';
           
             insert into directors values (NULL,'nm0005363','Guy Ritchie');
             select directorId into @did from directors where imdb='nm0005363';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0102494','My Own Private Idaho',1991);
          select movieId into @mid from movies where imdb = 'tt0102494';
           
             insert into directors values (NULL,'nm0001814','Gus Van Sant');
             select directorId into @did from directors where imdb='nm0001814';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0898367','The Road',2009);
          select movieId into @mid from movies where imdb = 'tt0898367';
           
             insert into directors values (NULL,'nm0384825','John Hillcoat');
             select directorId into @did from directors where imdb='nm0384825';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1080716','Karamazovi',2008);
          select movieId into @mid from movies where imdb = 'tt1080716';
           
             insert into directors values (NULL,'nm0954500','Petr Zelenka');
             select directorId into @did from directors where imdb='nm0954500';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1132620','Män som hatar kvinnor',2009);
          select movieId into @mid from movies where imdb = 'tt1132620';
           
             insert into directors values (NULL,'nm0649117','Niels Arden Oplev');
             select directorId into @did from directors where imdb='nm0649117';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0844479','The Collector',2009);
          select movieId into @mid from movies where imdb = 'tt0844479';
           
             insert into directors values (NULL,'nm1729303','Marcus Dunstan');
             select directorId into @did from directors where imdb='nm1729303';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0118849','Bacheha-Ye aseman (Children of Heaven)',1997);
          select movieId into @mid from movies where imdb = 'tt0118849';
           
             insert into directors values (NULL,'nm0006498','Majid Majidi');
             select directorId into @did from directors where imdb='nm0006498';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Family';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0468704','Deiji (Daisy)',2006);
          select movieId into @mid from movies where imdb = 'tt0468704';
           
             insert into directors values (NULL,'nm0490487','Wai-keung Lau');
             select directorId into @did from directors where imdb='nm0490487';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1151359','Leaves of Grass',2009);
          select movieId into @mid from movies where imdb = 'tt1151359';
           
             insert into directors values (NULL,'nm0625789','Tim Blake Nelson');
             select directorId into @did from directors where imdb='nm0625789';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0086250','Scarface',1983);
          select movieId into @mid from movies where imdb = 'tt0086250';
           
             insert into directors values (NULL,'nm0000361','Brian De Palma');
             select directorId into @did from directors where imdb='nm0000361';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0324554','Wicker Park',2004);
          select movieId into @mid from movies where imdb = 'tt0324554';
           
             insert into directors values (NULL,'nm0006476','Paul McGuigan');
             select directorId into @did from directors where imdb='nm0006476';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0115561','Le appartement',1996);
          select movieId into @mid from movies where imdb = 'tt0115561';
           
             insert into directors values (NULL,'nm0590813','Gilles Mimouni');
             select directorId into @did from directors where imdb='nm0590813';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1210042','Brooklyns Finest',2009);
          select movieId into @mid from movies where imdb = 'tt1210042';
           
             insert into directors values (NULL,'nm0298807','Antoine Fuqua');
             select directorId into @did from directors where imdb='nm0298807';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1461312','Alice',2009);
          select movieId into @mid from movies where imdb = 'tt1461312';
           
             insert into directors values (NULL,'nm0932216','Nick Willing');
             select directorId into @did from directors where imdb='nm0932216';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Fantasy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1135487','Duplicity',2009);
          select movieId into @mid from movies where imdb = 'tt1135487';
           
             insert into directors values (NULL,'nm0006904','Tony Gilroy');
             select directorId into @did from directors where imdb='nm0006904';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1289406','Harry Brown',2009);
          select movieId into @mid from movies where imdb = 'tt1289406';
           
             insert into directors values (NULL,'nm2905562','Daniel Barber');
             select directorId into @did from directors where imdb='nm2905562';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0364517','Jeux Denfants',2003);
          select movieId into @mid from movies where imdb = 'tt0364517';
           
             insert into directors values (NULL,'nm1367933','Yann Samuel');
             select directorId into @did from directors where imdb='nm1367933';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1303828','Defendor',2009);
          select movieId into @mid from movies where imdb = 'tt1303828';
           
             insert into directors values (NULL,'nm0824220','Peter Stebbings');
             select directorId into @did from directors where imdb='nm0824220';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1226273','Edge of Darkness',2010);
          select movieId into @mid from movies where imdb = 'tt1226273';
           
             insert into directors values (NULL,'nm0132709','Martin Campbell');
             select directorId into @did from directors where imdb='nm0132709';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0425123','Just Like Heaven',2005);
          select movieId into @mid from movies where imdb = 'tt0425123';
           
             insert into directors values (NULL,'nm0132709','Mark Waters');
             select directorId into @did from directors where imdb='nm0132709';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Fantasy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0212712','2046',2004);
          select movieId into @mid from movies where imdb = 'tt0212712';
           
             insert into directors values (NULL,'nm0939182','Kar Wai Wong');
             select directorId into @did from directors where imdb='nm0939182';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Fantasy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0765010','Brothers',2009);
          select movieId into @mid from movies where imdb = 'tt0765010';
           
             insert into directors values (NULL,'nm0006487','Jim Sheridan');
             select directorId into @did from directors where imdb='nm0006487';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'War';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1263670','Crazy Heart',2009);
          select movieId into @mid from movies where imdb = 'tt1263670';
           
             insert into directors values (NULL,'nm0178376','Scott Cooper');
             select directorId into @did from directors where imdb='nm0178376';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Music';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1130884','Shutter Island',2010);
          select movieId into @mid from movies where imdb = 'tt1130884';
           
             insert into directors values (NULL,'nm0000217','Martin Scorsese');
             select directorId into @did from directors where imdb='nm0000217';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1512894','Kiskanmak',2009);
          select movieId into @mid from movies where imdb = 'tt1512894';
           
             insert into directors values (NULL,'nm0218547','Zeki Demirkubuz');
             select directorId into @did from directors where imdb='nm0218547';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1149362','Das weisse Band - Eine deutsche Kindergeschichte
		',2009);
          select movieId into @mid from movies where imdb = 'tt1149362';
           
             insert into directors values (NULL,'nm0359734','Michael Haneke');
             select directorId into @did from directors where imdb='nm0359734';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0380510','The Lovely Bones',2009);
          select movieId into @mid from movies where imdb = 'tt0380510';
           
             insert into directors values (NULL,'nm0001392','Peter Jackson');
             select directorId into @did from directors where imdb='nm0001392';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Fantasy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1426361','Eastern Plays',2009);
          select movieId into @mid from movies where imdb = 'tt1426361';
           
             insert into directors values (NULL,'nm1189816','Kamen Kalev');
             select directorId into @did from directors where imdb='nm1189816';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0995039','Ghost Town',2008);
          select movieId into @mid from movies where imdb = 'tt0995039';
           
             insert into directors values (NULL,'nm0462895','David Koepp');
             select directorId into @did from directors where imdb='nm0462895';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Fantasy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0181875','Almost Famous',2000);
          select movieId into @mid from movies where imdb = 'tt0181875';
           
             insert into directors values (NULL,'nm0001081','Cameron Crowe');
             select directorId into @did from directors where imdb='nm0001081';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Music';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0381940','Cargo',2009);
          select movieId into @mid from movies where imdb = 'tt0381940';
           
             insert into directors values (NULL,'nm1393530','Ivan Engler');
             select directorId into @did from directors where imdb='nm1393530';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1319694','Glorious 39',2009);
          select movieId into @mid from movies where imdb = 'tt1319694';
           
             insert into directors values (NULL,'nm0689081','Stephen Poliakoff');
             select directorId into @did from directors where imdb='nm0689081';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'History';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'War';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1097643','Fifty Dead Men Walking',2009);
          select movieId into @mid from movies where imdb = 'tt1097643';
           
             insert into directors values (NULL,'nm0804556','Kari Skogland');
             select directorId into @did from directors where imdb='nm0804556';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0758751','Grey Gardens',2009);
          select movieId into @mid from movies where imdb = 'tt0758751';
           
             insert into directors values (NULL,'nm1302591','Michael Sucsy');
             select directorId into @did from directors where imdb='nm1302591';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1038919','The Bounty Hunter',2010);
          select movieId into @mid from movies where imdb = 'tt1038919';
           
             insert into directors values (NULL,'nm0855035','Andy Tennant');
             select directorId into @did from directors where imdb='nm0855035';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1065318','To Verdener',2008);
          select movieId into @mid from movies where imdb = 'tt1065318';
           
             insert into directors values (NULL,'nm0649117','Niels Arden Oplev');
             select directorId into @did from directors where imdb='nm0649117';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1558877','Vavien',2009);
          select movieId into @mid from movies where imdb = 'tt1558877';
           
             insert into directors values (NULL,'nm1533201','Durul Taylan');
             select directorId into @did from directors where imdb='nm1533201';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0032983','The Return of Frank James',1940);
          select movieId into @mid from movies where imdb = 'tt0032983';
           
             insert into directors values (NULL,'nm0000485','Fritz Lang');
             select directorId into @did from directors where imdb='nm0000485';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Western';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1483831','Lebanon',2009);
          select movieId into @mid from movies where imdb = 'tt1483831';
           
             insert into directors values (NULL,'nm1413443','Samuel Maoz');
             select directorId into @did from directors where imdb='nm1413443';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'War';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0417741','Harry Potter and the Half-Blood Prince',2009);
          select movieId into @mid from movies where imdb = 'tt0417741';
           
             insert into directors values (NULL,'nm0946734','David Yates');
             select directorId into @did from directors where imdb='nm0946734';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Family';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Fantasy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1224373','Slovenka',2009);
          select movieId into @mid from movies where imdb = 'tt1224373';
           
             insert into directors values (NULL,'nm0468995','Damjan Kozole');
             select directorId into @did from directors where imdb='nm0468995';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1361326','9:06',2009);
          select movieId into @mid from movies where imdb = 'tt1361326';
           
             insert into directors values (NULL,'nm0827459','Igor Sterk');
             select directorId into @did from directors where imdb='nm0827459';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1415283','Nanny McPhee and the Big Bang',2010);
          select movieId into @mid from movies where imdb = 'tt1415283';
           
             insert into directors values (NULL,'nm1264352','Susanna White');
             select directorId into @did from directors where imdb='nm1264352';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Family';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Fantasy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1179794','Timer',2009);
          select movieId into @mid from movies where imdb = 'tt1179794';
           
             insert into directors values (NULL,'nm2356614','Jac Schaeffer');
             select directorId into @did from directors where imdb='nm2356614';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Fantasy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1017451','The Runaways',2010);
          select movieId into @mid from movies where imdb = 'tt1017451';
           
             insert into directors values (NULL,'nm0797455','Floria Sigismondi');
             select directorId into @did from directors where imdb='nm0797455';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Music';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1385867','Cop out',2010);
          select movieId into @mid from movies where imdb = 'tt1385867';
           
             insert into directors values (NULL,'nm0003620','Kevin Smith');
             select directorId into @did from directors where imdb='nm0003620';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0445054','7 Days',2010);
          select movieId into @mid from movies where imdb = 'tt0445054';
           
             insert into directors values (NULL,'nm0343898','Daniel Grou');
             select directorId into @did from directors where imdb='nm0343898';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1594918','Eyyvah Eyvah',2010);
          select movieId into @mid from movies where imdb = 'tt1594918';
           
             insert into directors values (NULL,'nm1699659','Hakan Algul');
             select directorId into @did from directors where imdb='nm1699659';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1589424','Dersimiz: Ataturk',2010);
          select movieId into @mid from movies where imdb = 'tt1589424';
           
             insert into directors values (NULL,'nm1485677','Hamdi Alkan');
             select directorId into @did from directors where imdb='nm1485677';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Documentary';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'War';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1132620','The Girl with the Dragon Tattoo',2009);
          select movieId into @mid from movies where imdb = 'tt1132620';
           
             insert into directors values (NULL,'nm0649117','Niels Arden Oplev');
             select directorId into @did from directors where imdb='nm0649117';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriler';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0878835','Please Give',2010);
          select movieId into @mid from movies where imdb = 'tt0878835';
           
             insert into directors values (NULL,'nm0392237','Nicole Holofcener');
             select directorId into @did from directors where imdb='nm0392237';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1017460','Splice',2010);
          select movieId into @mid from movies where imdb = 'tt1017460';
           
             insert into directors values (NULL,'nm0622112','Vincenzo Natali');
             select directorId into @did from directors where imdb='nm0622112';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1590024','Eu cand vreau sa fluier, fluier',2010);
          select movieId into @mid from movies where imdb = 'tt1590024';
           
             insert into directors values (NULL,'nm2758521','Florin Servan');
             select directorId into @did from directors where imdb='nm2758521';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1470827','Monsters',2010);
          select movieId into @mid from movies where imdb = 'tt1470827';
           
             insert into directors values (NULL,'nm2284484','Gareth Edwards');
             select directorId into @did from directors where imdb='nm2284484';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0435761','Toy Story 3',2010);
          select movieId into @mid from movies where imdb = 'tt0435761';
           
             insert into directors values (NULL,'nm0881279','Lee Unkrich');
             select directorId into @did from directors where imdb='nm0881279';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Animation';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Family';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Fantasy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0429493','The A-Team',2010);
          select movieId into @mid from movies where imdb = 'tt0429493';
           
             insert into directors values (NULL,'nm0138620','Joe Carnahan');
             select directorId into @did from directors where imdb='nm0138620';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0429493','Knight and Day',2010);
          select movieId into @mid from movies where imdb = 'tt0429493';
           
             insert into directors values (NULL,'nm0003506','James Mangold');
             select directorId into @did from directors where imdb='nm0003506';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1320253','The Expendables',2010);
          select movieId into @mid from movies where imdb = 'tt1320253';
           
             insert into directors values (NULL,'nm0000230','Sylvester Stallone');
             select directorId into @did from directors where imdb='nm0000230';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0947810','Green Zone',2010);
          select movieId into @mid from movies where imdb = 'tt0947810';
           
             insert into directors values (NULL,'nm0339030','Paul Greengrass');
             select directorId into @did from directors where imdb='nm0339030';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'War';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1235796','Ondine',2009);
          select movieId into @mid from movies where imdb = 'tt1235796';
           
             insert into directors values (NULL,'nm0001403','Neil Jordan');
             select directorId into @did from directors where imdb='nm0001403';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1216492','Leap Year',2010);
          select movieId into @mid from movies where imdb = 'tt1216492';
           
             insert into directors values (NULL,'nm0875793','Anand Tucker');
             select directorId into @did from directors where imdb='nm0875793';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1313092','Animal Kingdom',2010);
          select movieId into @mid from movies where imdb = 'tt1313092';
           
             insert into directors values (NULL,'nm2391575','David Michôd');
             select directorId into @did from directors where imdb='nm2391575';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0892769','How to Train Your Dragon',2010);
          select movieId into @mid from movies where imdb = 'tt0892769';
           
             insert into directors values (NULL,'nm0213450','Dean DeBlois');
             select directorId into @did from directors where imdb='nm0213450';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
           
             insert into directors values (NULL,'nm0761498','Chris Sanders');
             select directorId into @did from directors where imdb='nm0761498';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Animation';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Family';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1386683','En ganske snill mann (A Somewhat Gentle Man)',2010);
          select movieId into @mid from movies where imdb = 'tt1386683';
           
             insert into directors values (NULL,'nm0596407','Hans Petter Moland');
             select directorId into @did from directors where imdb='nm0596407';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1542852','Carancho',2010);
          select movieId into @mid from movies where imdb = 'tt1542852';
           
             insert into directors values (NULL,'nm0871086','Pablo Trapero');
             select directorId into @did from directors where imdb='nm0871086';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1121977','Mother and Child',2009);
          select movieId into @mid from movies where imdb = 'tt1121977';
           
             insert into directors values (NULL,'nm0006554','Rodrigo Garcia');
             select directorId into @did from directors where imdb='nm0006554';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1156143','Wild Grass',2009);
          select movieId into @mid from movies where imdb = 'tt1156143';
           
             insert into directors values (NULL,'nm0720297','Alain Resnais');
             select directorId into @did from directors where imdb='nm0720297';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1399683','Winter s Bone',2010);
          select movieId into @mid from movies where imdb = 'tt1399683';
           
             insert into directors values (NULL,'nm0335138','Debra Granik');
             select directorId into @did from directors where imdb='nm0335138';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0435761','Toy Story 3',2010);
          select movieId into @mid from movies where imdb = 'tt0435761';
           
             insert into directors values (NULL,'nm0881279','Lee Unkrich');
             select directorId into @did from directors where imdb='nm0881279';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Animation';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Family';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Fantasy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1465487','L arnacoeur',2010);
          select movieId into @mid from movies where imdb = 'tt1465487';
           
             insert into directors values (NULL,'nm0154312','Pascal Chaumeil');
             select directorId into @did from directors where imdb='nm0154312';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1375666','Inception',2010);
          select movieId into @mid from movies where imdb = 'tt1375666';
           
             insert into directors values (NULL,'nm0634240','Christopher Nolan');
             select directorId into @did from directors where imdb='nm0634240';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1284591','Süt',2008);
          select movieId into @mid from movies where imdb = 'tt1284591';
           
             insert into directors values (NULL,'nm0969427','Semih Kaplanoglu');
             select directorId into @did from directors where imdb='nm0969427';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1571724','Bal',2010);
          select movieId into @mid from movies where imdb = 'tt1571724';
           
             insert into directors values (NULL,'nm0969427','Semih Kaplanoglu');
             select directorId into @did from directors where imdb='nm0969427';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1021004','Yumurta',2007);
          select movieId into @mid from movies where imdb = 'tt1021004';
           
             insert into directors values (NULL,'nm0969427','Semih Kaplanoglu');
             select directorId into @did from directors where imdb='nm0969427';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0171804','Boys Don t Cry',1999);
          select movieId into @mid from movies where imdb = 'tt0171804';
           
             insert into directors values (NULL,'nm0005303','Kimberly Peirce');
             select directorId into @did from directors where imdb='nm0005303';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1194417','Casino Jack',2010);
          select movieId into @mid from movies where imdb = 'tt1194417';
           
             insert into directors values (NULL,'nm0382584','George Hickenlooper');
             select directorId into @did from directors where imdb='nm0382584';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1235189','Wild Target',2010);
          select movieId into @mid from movies where imdb = 'tt1235189';
           
             insert into directors values (NULL,'nm0528718','Jonathan Lynn');
             select directorId into @did from directors where imdb='nm0528718';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0133093','The Matrix',1999);
          select movieId into @mid from movies where imdb = 'tt0133093';
           
             insert into directors values (NULL,'nm0905152','Andy Wachowski');
             select directorId into @did from directors where imdb='nm0905152';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
           
             insert into directors values (NULL,'nm0905154','Lana Wachowski');
             select directorId into @did from directors where imdb='nm0905154';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0234215','The Matrix Reloaded',2003);
          select movieId into @mid from movies where imdb = 'tt0234215';
           
             insert into directors values (NULL,'nm0905152','Andy Wachowski');
             select directorId into @did from directors where imdb='nm0905152';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
           
             insert into directors values (NULL,'nm0905154','Lana Wachowski');
             select directorId into @did from directors where imdb='nm0905154';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0242653','The Matrix Revolutions',2003);
          select movieId into @mid from movies where imdb = 'tt0242653';
           
             insert into directors values (NULL,'nm0905152','Andy Wachowski');
             select directorId into @did from directors where imdb='nm0905152';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
           
             insert into directors values (NULL,'nm0905154','Lana Wachowski');
             select directorId into @did from directors where imdb='nm0905154';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1242422','Cell 211',2009);
          select movieId into @mid from movies where imdb = 'tt1242422';
           
             insert into directors values (NULL,'nm0905152','Daniel Monzón');
             select directorId into @did from directors where imdb='nm0905152';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1587278','South Solitary',2010);
          select movieId into @mid from movies where imdb = 'tt1587278';
           
             insert into directors values (NULL,'nm0057284','Shirley Barrett');
             select directorId into @did from directors where imdb='nm0057284';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1440728','The American',2010);
          select movieId into @mid from movies where imdb = 'tt1440728';
           
             insert into directors values (NULL,'nm0179221','Anton Corbijn');
             select directorId into @did from directors where imdb='nm0179221';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0840361','The Town',2010);
          select movieId into @mid from movies where imdb = 'tt0840361';
           
             insert into directors values (NULL,'nm0000255','Ben Affleck');
             select directorId into @did from directors where imdb='nm0000255';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0889573','The Switch',2010);
          select movieId into @mid from movies where imdb = 'tt0889573';
           
             insert into directors values (NULL,'nm0330347','Josh Gordon');
             select directorId into @did from directors where imdb='nm0330347';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
           
             insert into directors values (NULL,'nm0330347','Will Speck');
             select directorId into @did from directors where imdb='nm0330347';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1314655','Devil',2010);
          select movieId into @mid from movies where imdb = 'tt1314655';
           
             insert into directors values (NULL,'nm0235719','John Erick Dowdle');
             select directorId into @did from directors where imdb='nm0235719';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1285016','The Social Network',2010);
          select movieId into @mid from movies where imdb = 'tt1285016';
           
             insert into directors values (NULL,'nm0000399','David Fincher');
             select directorId into @did from directors where imdb='nm0000399';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = '>History';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1421051','Somewhere',2010);
          select movieId into @mid from movies where imdb = 'tt1421051';
           
             insert into directors values (NULL,'nm0001068','Sofia Coppola');
             select directorId into @did from directors where imdb='nm0001068';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1462758','Buried',2010);
          select movieId into @mid from movies where imdb = 'tt1462758';
           
             insert into directors values (NULL,'nm0181579','Rodrigo Cortes');
             select directorId into @did from directors where imdb='nm0181579';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1401143','Rare Exports: A Christmas Tale',2010);
          select movieId into @mid from movies where imdb = 'tt1401143';
           
             insert into directors values (NULL,'nm1118351','Jalmari Helander');
             select directorId into @did from directors where imdb='nm1118351';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Fantasy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0446029','Scott Pilgrim vs. the World',2010);
          select movieId into @mid from movies where imdb = 'tt0446029';
           
             insert into directors values (NULL,'nm0942367','Edgar Wright');
             select directorId into @did from directors where imdb='nm0942367';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Fantasy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1322385','Submarino',2010);
          select movieId into @mid from movies where imdb = 'tt1322385';
           
             insert into directors values (NULL,'nm0899121','Thomas Vinterberg');
             select directorId into @did from directors where imdb='nm0899121';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1588895','Uncle Boonmee Who Can Recall His Past Lives',2010);
          select movieId into @mid from movies where imdb = 'tt1588895';
           
             insert into directors values (NULL,'nm0917405','Apichatpong Weerasethakul');
             select directorId into @did from directors where imdb='nm0917405';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Fantasy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1456941','Tomorrow, When the War Began',2010);
          select movieId into @mid from movies where imdb = 'tt1456941';
           
             insert into directors values (NULL,'nm0064181','Stuart Beattie');
             select directorId into @did from directors where imdb='nm0064181';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1496005','The Tree',2010);
          select movieId into @mid from movies where imdb = 'tt1496005';
           
             insert into directors values (NULL,'nm0078465','Julie Bertucelli');
             select directorId into @did from directors where imdb='nm0078465';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1334260','Never Let Me Go',2010);
          select movieId into @mid from movies where imdb = 'tt1334260';
           
             insert into directors values (NULL,'nm0738796','Mark Romanek');
             select directorId into @did from directors where imdb='nm0738796';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Sci-Fi';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1423995','Stone',2010);
          select movieId into @mid from movies where imdb = 'tt1423995';
           
             insert into directors values (NULL,'nm0192845','John Curran');
             select directorId into @did from directors where imdb='nm0192845';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1339161','The Robber',2010);
          select movieId into @mid from movies where imdb = 'tt1339161';
           
             insert into directors values (NULL,'nm0374663','Benjamin Heisenberg');
             select directorId into @did from directors where imdb='nm0374663';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1187044','The Maid',2009);
          select movieId into @mid from movies where imdb = 'tt1187044';
           
             insert into directors values (NULL,'nm2928364','Sebastian Silva');
             select directorId into @did from directors where imdb='nm2928364';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1527788','The Man From Nowhere',2010);
          select movieId into @mid from movies where imdb = 'tt1527788';
           
             insert into directors values (NULL,'nm2556572','Jeong-beom Lee');
             select directorId into @did from directors where imdb='nm2556572';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1646926','Open Season 3',2010);
          select movieId into @mid from movies where imdb = 'tt1646926';
           
             insert into directors values (NULL,'nm0970606','Cody Cameron');
             select directorId into @did from directors where imdb='nm0970606';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Animation';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0485947','Mr. Nobody',2009);
          select movieId into @mid from movies where imdb = 'tt0485947';
           
             insert into directors values (NULL,'nm0233757','Jaco Van Dormael');
             select directorId into @did from directors where imdb='nm0233757';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Fantasy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0879870','Eat Pray Love',2010);
          select movieId into @mid from movies where imdb = 'tt0879870';
           
             insert into directors values (NULL,'nm0614682','Ryan Murphy');
             select directorId into @did from directors where imdb='nm0614682';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1278379','Jack Goes Boating',2010);
          select movieId into @mid from movies where imdb = 'tt1278379';
           
             insert into directors values (NULL,'nm0000450','Philip Seymour Hoffman');
             select directorId into @did from directors where imdb='nm0000450';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1320082','The Concert',2009);
          select movieId into @mid from movies where imdb = 'tt1320082';
           
             insert into directors values (NULL,'nm0586123','Radu Mihaileanu');
             select directorId into @did from directors where imdb='nm0586123';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Music';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1148200','Chain Letter',2010);
          select movieId into @mid from movies where imdb = 'tt1148200';
           
             insert into directors values (NULL,'nm1938064','Deon Taylor');
             select directorId into @did from directors where imdb='nm1938064';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0804497','It is Kind of a Funny Story',2010);
          select movieId into @mid from movies where imdb = 'tt0804497';
           
             insert into directors values (NULL,'nm1349818','Anna Boden');
             select directorId into @did from directors where imdb='nm1349818';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
           
             insert into directors values (NULL,'nm0281396','Ryan Fleck');
             select directorId into @did from directors where imdb='nm0281396';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0758752','Love and Other Drugs',2010);
          select movieId into @mid from movies where imdb = 'tt0758752';
           
             insert into directors values (NULL,'nm0001880','Edward Zwick');
             select directorId into @did from directors where imdb='nm0001880';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0977855','Fair Game',2010);
          select movieId into @mid from movies where imdb = 'tt0977855';
           
             insert into directors values (NULL,'nm0510731','Daug Liman');
             select directorId into @did from directors where imdb='nm0510731';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1504320','Due Date',2010);
          select movieId into @mid from movies where imdb = 'tt1504320';
           
             insert into directors values (NULL,'nm0680846','Todd Phillips');
             select directorId into @did from directors where imdb='nm0680846';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1504320','The Kings Speech',2010);
          select movieId into @mid from movies where imdb = 'tt1504320';
           
             insert into directors values (NULL,'nm0393799','Tom Hooper');
             select directorId into @did from directors where imdb='nm0393799';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'History';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0842926','The Kids are All Right',2010);
          select movieId into @mid from movies where imdb = 'tt0842926';
           
             insert into directors values (NULL,'nm0158966','Lisa Cholodenko');
             select directorId into @did from directors where imdb='nm0158966';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1323594','Despicable Me',2010);
          select movieId into @mid from movies where imdb = 'tt1323594';
           
             insert into directors values (NULL,'nm1853544','Pierre Coffin');
             select directorId into @did from directors where imdb='nm1853544';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
           
             insert into directors values (NULL,'nm0719208','Schris Renaud');
             select directorId into @did from directors where imdb='nm0719208';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Animation';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Family';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1441373','The Solitude of Prime Numbers',2010);
          select movieId into @mid from movies where imdb = 'tt1441373';
           
             insert into directors values (NULL,'nm0182459','Saverio Costanzo');
             select directorId into @did from directors where imdb='nm0182459';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1024942','Kærlighed på film',2007);
          select movieId into @mid from movies where imdb = 'tt1024942';
           
             insert into directors values (NULL,'nm0097079','Ole Bornedal');
             select directorId into @did from directors where imdb='nm0097079';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1182350','You Will Meet a Tall Dark Stranger',2010);
          select movieId into @mid from movies where imdb = 'tt1182350';
           
             insert into directors values (NULL,'nm0000095','Woody Allen');
             select directorId into @did from directors where imdb='nm0000095';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0981042','Like Dandelion Dust',2009);
          select movieId into @mid from movies where imdb = 'tt0981042';
           
             insert into directors values (NULL,'nm0348197','Jon Gunn');
             select directorId into @did from directors where imdb='nm0348197';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Family';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1523498','The Passion',2010);
          select movieId into @mid from movies where imdb = 'tt1523498';
           
             insert into directors values (NULL,'nm0563473','Carlo Mazzacurati');
             select directorId into @did from directors where imdb='nm0563473';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0817177','Flipped',2010);
          select movieId into @mid from movies where imdb = 'tt0817177';
           
             insert into directors values (NULL,'nm0001661','Rob Reiner');
             select directorId into @did from directors where imdb='nm0001661';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0802948','An American Crime',2007);
          select movieId into @mid from movies where imdb = 'tt0802948';
           
             insert into directors values (NULL,'nm0641373','Tommy OHaver');
             select directorId into @did from directors where imdb='nm0641373';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0798817','13',2010);
          select movieId into @mid from movies where imdb = 'tt0798817';
           
             insert into directors values (NULL,'nm1201875','Géla Babluani');
             select directorId into @did from directors where imdb='nm1201875';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1242432','I Spit on Your Grave',2010);
          select movieId into @mid from movies where imdb = 'tt1242432';
           
             insert into directors values (NULL,'nm0003458','Steven R. Monroe');
             select directorId into @did from directors where imdb='nm0003458';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1179034','From Paris with Love',2010);
          select movieId into @mid from movies where imdb = 'tt1179034';
           
             insert into directors values (NULL,'nm0603628','Pierre Morel');
             select directorId into @did from directors where imdb='nm0603628';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1075110','The Misfortunates',2009);
          select movieId into @mid from movies where imdb = 'tt1075110';
           
             insert into directors values (NULL,'nm0886976','Felix Van Groeningen');
             select directorId into @did from directors where imdb='nm0886976';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1028576','Secretariat',2010);
          select movieId into @mid from movies where imdb = 'tt1028576';
           
             insert into directors values (NULL,'nm0908824','Randall Wallace');
             select directorId into @did from directors where imdb='nm0908824';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Family';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'History';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1055292','Life as We Know It',2010);
          select movieId into @mid from movies where imdb = 'tt1055292';
           
             insert into directors values (NULL,'nm0075528','Greg Berlanti');
             select directorId into @did from directors where imdb='nm0075528';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1228987','Let Me In',2010);
          select movieId into @mid from movies where imdb = 'tt1228987';
           
             insert into directors values (NULL,'nm0716257','Matt Reeves');
             select directorId into @did from directors where imdb='nm0716257';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Fantasy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Horror';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1244754','Conviction',2010);
          select movieId into @mid from movies where imdb = 'tt1244754';
           
             insert into directors values (NULL,'nm0001282','Tony Goldwyn');
             select directorId into @did from directors where imdb='nm0001282';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Biography';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1194263','Get Low',2010);
          select movieId into @mid from movies where imdb = 'tt1194263';
           
             insert into directors values (NULL,'nm0773689','Aaron Schneider');
             select directorId into @did from directors where imdb='nm0773689';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Mystery';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1183911','Mr. Nice',2010);
          select movieId into @mid from movies where imdb = 'tt1183911';
           
             insert into directors values (NULL,'nm0741262','Bernard Rose');
             select directorId into @did from directors where imdb='nm0741262';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1403988','The Romantics',2010);
          select movieId into @mid from movies where imdb = 'tt1403988';
           
             insert into directors values (NULL,'nm0630633','Galt Niederhoffer');
             select directorId into @did from directors where imdb='nm0630633';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1196141','Diary of a Wimpy Kid',2010);
          select movieId into @mid from movies where imdb = 'tt1196141';
           
             insert into directors values (NULL,'nm0294457','Thor Freudenthal');
             select directorId into @did from directors where imdb='nm0294457';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Family';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1542344','127 Hours',2010);
          select movieId into @mid from movies where imdb = 'tt1542344';
           
             insert into directors values (NULL,'nm0000965','Danny Boyle');
             select directorId into @did from directors where imdb='nm0000965';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Adventure';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Thriller';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1234654','Greenberg',2010);
          select movieId into @mid from movies where imdb = 'tt1234654';
           
             insert into directors values (NULL,'nm0000876','Noah Baumbach');
             select directorId into @did from directors where imdb='nm0000876';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0997035','Bibliothèque Pascal',2010);
          select movieId into @mid from movies where imdb = 'tt0997035';
           
             insert into directors values (NULL,'nm0354451','Szabolcs Hajdu');
             select directorId into @did from directors where imdb='nm0354451';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt0985694','Machete',2010);
          select movieId into @mid from movies where imdb = 'tt0985694';
           
             insert into directors values (NULL,'nm0542450','Ethan Maniquis');
             select directorId into @did from directors where imdb='nm0542450';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
           
             insert into directors values (NULL,'nm0001675','Robert Rodríguez');
             select directorId into @did from directors where imdb='nm0001675';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Comedy';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1458175','The Next Three Days',2010);
          select movieId into @mid from movies where imdb = 'tt1458175';
           
             insert into directors values (NULL,'nm0353673','Paul Haggis');
             select directorId into @did from directors where imdb='nm0353673';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Crime';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1510938','The Sunset Limited',2011);
          select movieId into @mid from movies where imdb = 'tt1510938';
           
             insert into directors values (NULL,'nm0000169','Tommy Lee Jones');
             select directorId into @did from directors where imdb='nm0000169';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
          insert into movies(imdb,title,`year`) values ('tt1243957','The Tourist',2010);
          select movieId into @mid from movies where imdb = 'tt1243957';
           
             insert into directors values (NULL,'nm0003697','Florian Henckel von Donnersmarck');
             select directorId into @did from directors where imdb='nm0003697';
             insert into moviedirectors(movieId,directorId) values (@mid,@did);  
          
             select genreId into @gid from genres where name = 'Action';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Drama';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          
             select genreId into @gid from genres where name = 'Romance';
             insert into moviegenres(movieId,genreId) values (@mid,@gid);
          